pub fn longitud_de_cadenas(vector:[String;5]) -> [i32;5] {
    let mut vec_enteros: [i32;5] = [0,0,0,0,0]; 

    for i in 0..5 {
        //vec_enteros[i] = vector[i].chars().count(); //.count devuelve un entero referenciando la longitud del string.
    }

    return vec_enteros;
}